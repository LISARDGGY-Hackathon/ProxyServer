[project]
name = "proxyserver"
version = "0.1.0"
description = "Proxy server for AMD AI Agent Hackathon"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
	"fastapi>=0.95.0",
	"uvicorn[standard]>=0.22.0",
	"httpx>=0.24.0",
]
